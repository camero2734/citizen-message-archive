---
import { type Props as CardProps } from './Card.astro';
import Clue from './Clue.astro';
import Text from './Text.astro';

export interface Props {
  numbers: string[];
}

const { numbers } = Astro.props;

---

<li class="group">
  <ol class="group-clues">
    {numbers.map((number) => (
      <Clue clue="" number={number} hideText={true} color="#F7C622" />
      <div class="dash"></div>
    ))}
  </ol>
</li>

<style>

.group-clues {
  display: flex;
  list-style: none;
  padding: 0;
}

.dash {
  margin: auto 0;
  width: 20px;
  height: 4px;
  background-color: #F7C622;
}

.dash:last-of-type {
  display: none;
}

</style>
